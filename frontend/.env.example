# Mapbox public access token — required for map tiles.
# Create a free account at https://account.mapbox.com and copy your default public token.
# See README.md → "Mapbox Setup" for full instructions.
VITE_MAPBOX_TOKEN=pk.your-mapbox-token-here
